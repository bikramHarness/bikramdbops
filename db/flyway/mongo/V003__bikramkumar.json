{
  "collMod": "users2",
  "validator": {
    "$jsonSchema": {
      "bsonType": "object",
      "required": ["email", "name", "country"],
      "properties": {
        "email": { "bsonType": "string" },
        "name":  { "bsonType": "string" },
        "country": { "bsonType": "string" }
      }
    }
  }
}
